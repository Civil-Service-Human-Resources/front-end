{% extends 'layouts/library.layout.njk' %}

{% set pageTitle = 'MOT Library - Ajax Browse' %}

{% block content %}

  {{ components.heading(text='Ajax Browse', tag='h2', size='medium') }}

  {{ components.paragraph(text='This component is still under development.', lead=true) }}

  {{ components.placeholderAjaxBrowse(activeItems=1, inactiveItems=0, modifier='one-block') }}

  {{ components.placeholderAjaxBrowse(activeItems=1, inactiveItems=1, modifier='two-blocks') }}

  {# {{ components.placeholderAjaxBrowse(activeItems=1, inactiveItems=2, modifier='three-blocks') }}

  {{ components.placeholderAjaxBrowse(activeItems=1, inactiveItems=3, modifier='four-blocks') }} #}

{% endblock %}